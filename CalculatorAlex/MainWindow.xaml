<Window x:Class="CalculatorAlex.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalculatorAlex"
        mc:Ignorable="d"
        Title="Голосовой калькулятор" Height="434" Width="716">
    <Window.Resources>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Width" Value="90"/>
            <Setter Property="Margin" Value="0,10,0,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter x:Name="PART_Content"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Button.IsMouseOver" Value="True">
                    <Setter Property="Button.Height" Value="70"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Background="Black">
        <Image Source="background.jpg"/>
        <Grid Margin="50,0,50,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.18*"/>
                <RowDefinition Height="0.4*"/>
                <RowDefinition Height="0.18*"/>
                <RowDefinition/>
                <RowDefinition Height="0.35*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="DodgerBlue" Opacity="0.8"/>
            <TextBlock Grid.Row="0" Text="Выражение:" TextAlignment="Center" Foreground="White" FontSize="30" />
            <Grid Grid.Row="1" Background="SteelBlue" Opacity="0.95"/>
            <ScrollViewer Grid.Row="1">
                <TextBlock x:Name="outputSpeech"  TextAlignment="Center" FontSize="25" Foreground="White" TextWrapping="Wrap"/>
            </ScrollViewer>
            <Grid Grid.Row="2" Background="DodgerBlue" Opacity="0.75"/>
            <TextBlock Text="Вычисления:" Grid.Row="2" TextAlignment="Center" Foreground="White" FontSize="30"/>
            <Grid Grid.Row="3" Background="SteelBlue" Opacity="0.95"/>
            <ScrollViewer Grid.Row="3">
                <TextBlock x:Name="outputCalculation" Grid.Row="3" TextAlignment="Center" FontSize="25" Foreground="White" TextWrapping="Wrap" />
            </ScrollViewer>
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.5*"/>
                    <ColumnDefinition Width="0.5*"/>
                </Grid.ColumnDefinitions>
                <Grid Background="DodgerBlue" Opacity="0.75" Grid.ColumnSpan="2"/>
                <Button x:Name="write" Grid.Row="4" Grid.Column="0" Click="write_Click">
                    <Button.Background>
                        <ImageBrush ImageSource="micro.png"/>
                    </Button.Background>
                </Button>
                <Button x:Name="clear" Grid.Row="4" Grid.Column="1" Click="clear_Click">
                    <Button.Background>
                        <ImageBrush ImageSource="list.png"/>
                    </Button.Background>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</Window>
