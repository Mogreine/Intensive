<Window x:Class="CalculatorAlex.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalculatorAlex"
        mc:Ignorable="d"
        Title="Голосовой калькулятор" Height="434" Width="716">
    <Window.Resources>
        <Style x:Key="microButton" TargetType="Button">
            <Setter Property="Height" Value="65"/>
            <Setter Property="Width" Value="85"/>
            <Setter Property="Margin" Value="0,10,0,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background">
                <Setter.Value>
                    <ImageBrush ImageSource="micro.png"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter x:Name="PART_Content"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Height" Value="85"/>
                    <Setter Property="Width" Value="100"/>
                    <Setter Property="Background">
                        <Setter.Value>
                            <ImageBrush ImageSource="micro1.png"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="listButton" TargetType="Button">
            <Setter Property="Height" Value="65"/>
            <Setter Property="Width" Value="85"/>
            <Setter Property="Margin" Value="0,10,0,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background">
                <Setter.Value>
                    <ImageBrush ImageSource="list.png"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter x:Name="PART_Content"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Height" Value="85"/>
                    <Setter Property="Width" Value="100"/>
                    <Setter Property="Background">
                        <Setter.Value>
                            <ImageBrush ImageSource="list1.png"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Background="Black">
        <Image Source="background.jpg"/>
        <Grid Margin="50,0,50,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.18*"/>
                <RowDefinition Height="0.4*"/>
                <RowDefinition Height="0.18*"/>
                <RowDefinition/>
                <RowDefinition Height="0.35*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="SteelBlue"/>
            <TextBlock Grid.Row="0" Text="Выражение:" TextAlignment="Center" Foreground="White" FontSize="30" />
            <Grid Grid.Row="1" Background="DodgerBlue" Opacity="0.75"/>
            <ScrollViewer Grid.Row="1">
                <TextBlock x:Name="outputSpeech"  TextAlignment="Center" FontSize="25" Foreground="White" TextWrapping="Wrap"/>
            </ScrollViewer>
            <Grid Grid.Row="2" Background="SteelBlue"/>
            <TextBlock Text="Вычисления:" Grid.Row="2" TextAlignment="Center" Foreground="White" FontSize="30"/>
            <Grid Grid.Row="3" Background="DodgerBlue" Opacity="0.75"/>
            <ScrollViewer Grid.Row="3">
                <TextBlock x:Name="outputCalculation" Grid.Row="3" TextAlignment="Center" FontSize="25" Foreground="White" TextWrapping="Wrap" />
            </ScrollViewer>
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.5*"/>
                    <ColumnDefinition Width="0.5*"/>
                </Grid.ColumnDefinitions>
                <Grid Background="SteelBlue" Grid.ColumnSpan="2"/>
                <Button x:Name="write" Style="{StaticResource microButton}" Grid.Row="4" Grid.Column="0" Click="write_Click"/>
                <Button x:Name="clear" Style="{StaticResource listButton}"  Grid.Row="4" Grid.Column="1" Click="clear_Click">
                </Button>
            </Grid>
        </Grid>
    </Grid>
</Window>
